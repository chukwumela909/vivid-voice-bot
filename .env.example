# ── WorldStreet Vivid voice bot ──────────────────────────────────────────────
# Copy to bot/.env and fill in. These keys live ONLY on the bot; the browser
# never receives them.

# Speech-to-text (Deepgram)
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-3
DEEPGRAM_ENDPOINTING=250

# LLM — switchable. LLM_PROVIDER=groq (default), openai, or openrouter.
LLM_PROVIDER=groq
LLM_TEMPERATURE=0.4
LLM_MAX_TOKENS=320
# Groq (default provider)
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
# OpenAI (only if LLM_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
# OpenRouter (only if LLM_PROVIDER=openrouter). Reuses OPEN_ROUTER_KEY below.
# Model must support tool calling — e.g. x-ai/grok-4.20.
OPENROUTER_MODEL=x-ai/grok-4.20

# Text-to-speech — switchable. TTS_PROVIDER=elevenlabs (default) or cartesia.
TTS_PROVIDER=elevenlabs

# ElevenLabs (default TTS). The browser's voice menu picks the voice per session
# (?voice=); ELEVENLABS_VOICE_ID is the fallback when none is selected.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=cgSgspJ2msm6clMCkdW9
ELEVENLABS_MODEL=eleven_flash_v2_5
# Expressivity is OFF by default — the streaming WS rejects changing voice_settings
# (1008 policy violation). Only set these if your voice/model tolerates it.
ELEVENLABS_STYLE=
ELEVENLABS_STABILITY=
ELEVENLABS_SPEAKER_BOOST=

# Cartesia (only if TTS_PROVIDER=cartesia)
CARTESIA_API_KEY=
CARTESIA_VOICE_ID=71a7ad14-091c-4e8e-a314-022ece01c121

# Turn-taking (Silero VAD)
VAD_STOP_SECS=0.5
VAD_START_SECS=0.2

# Tool bridge — how long the bot waits for the browser to run a tool.
TOOL_TIMEOUT_SECS=20

# Vision (the `look` tool — Vivid seeing through the user's camera/screen).
# Runs on OpenRouter so image tokens don't hit the conversational LLM's budget.
# REQUIRED for vision: get a key at https://openrouter.ai/keys
OPEN_ROUTER_KEY=
VISION_MODEL=google/gemini-2.5-flash
VISION_MAX_TOKENS=220
VISION_TIMEOUT_SECS=25
# How long to wait for the browser to return captured frames.
FRAME_TIMEOUT_SECS=15
# Frames captured per glance — a short burst read as one live view (not a stale snapshot).
VISION_FRAME_COUNT=5

# Context auto-summarization (token-budget control)
SUMMARIZE_AT_TOKENS=4000
SUMMARIZE_AT_MESSAGES=24
SUMMARY_TARGET_TOKENS=1500
SUMMARY_KEEP_MESSAGES=6

# Transport: "smallwebrtc" (default, P2P, local dev) or "daily" (managed, cloud).
TRANSPORT=smallwebrtc
DAILY_API_KEY=
# JSON array of STUN/TURN servers (must match the web app's NEXT_PUBLIC_WEBRTC_ICE_SERVERS).
WEBRTC_ICE_SERVERS=["stun:stun.l.google.com:19302"]

# CORS — comma-separated web origins allowed to reach the bot, or "*" for dev.
ALLOWED_ORIGINS=*

# Signaling port
BOT_PORT=7860
